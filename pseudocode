pseudo code:

execute each night at 1 am CET
for each website
open website
	for each article starting from the top:
		open sub page of article
		is this article the same as top article from yesterday?
			if yes: break loop
			if no: continue
		check whether key words are contained
			if no: go to next
			if yes: save page and link in data base
		extract information on
			city (double check with full list of German cities)
			warnstreik or streik (check whether warnstreik in body)
			number of strikers (check for numbers?)
			length (check for "hours", "days", ...)
send e-mail: "there are X new articles"
